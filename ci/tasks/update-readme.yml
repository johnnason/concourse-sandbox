inputs:
  - name: last-commit-message
platform: linux
image_resource:
  type: docker-image
  source:
    repository: samueldebruyn/debian-git
run:
  path: bash
  args:
    - -c
    - |
      set -eux
      ls
      cd last-commit-message/output
      cat last_commit.txt
